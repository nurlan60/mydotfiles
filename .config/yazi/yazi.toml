[plugin]
prepend_previewers = [
  # { name = "*.md", run = "glow" },

  { name = "*/", run = "eza-preview" },

  # Doc previewer
  { name = "*.doc", run = "doc" },

  # Docx previewer
  { name = "*.docx", run = "docx" },

  # Archive previewer
  { mime = "application/{*zip,tar,bzip*,7z-compressed,xz,rar}", run = "ouch" },
]
